
  # Bang guling

  This is a code bundle for Bang guling. The original project is available at https://www.figma.com/design/toVMSk8anEI7620CnEGKeB/Bang-guling.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  